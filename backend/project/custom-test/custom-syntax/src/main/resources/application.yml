server:
  port: 9002

spring:
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  config:
    use-legacy-processing: true
  profiles:
    active: dev
  application:
    name: custom-syntax
